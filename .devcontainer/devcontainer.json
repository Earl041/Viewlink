{
  "name": "My Dev Container",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "settings": {},
  "extensions": [],
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y chromium-browser chromium-chromedriver && pip install -r requirements.txt",
  "remoteUser": "vscode"
}
